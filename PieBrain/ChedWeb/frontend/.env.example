# Backend API URL
# In production, the frontend needs to know where the backend is
# Leave empty for same-origin (when using nginx proxy)
# Set to http://localhost:8000 for development
# Set to http://<raspberry-pi-ip>:8000 for production with separate ports
VITE_API_URL=http://localhost:8000
